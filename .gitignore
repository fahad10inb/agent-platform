# Python
__pycache__/
*.pyc
.venv/
venv/

# Local secrets — NEVER commit real keys. We commit .env.example instead.
.env

# Local database file (data, not code)
*.db

# Generated eval reports (a demo artifact, not code)
backend/reports/

# Editor / OS
.DS_Store
.idea/
.vscode/
